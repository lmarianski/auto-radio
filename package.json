{
	"scripts": {
		"build": "yarn workspaces foreach -pvi run build",
		"dev": "yarn workspaces foreach -pvi run dev"
	},
	"workspaces": [
		"./packages/*"
	],
	"devDependencies": {
		"eslint": "^7.28.0",
		"typescript": "^4.3.2"
	}
}
